# M7_Project — Environment Configuration
# Copy this to .env and fill in your values

# Database
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=charco_db

# App
APP_URL=http://localhost/M7_Project/
APP_NAME="DashBoard ESP"
APP_ENV=development

# Session
SESSION_LIFETIME=7200

# SMTP (for password reset emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@dashboard.pt
